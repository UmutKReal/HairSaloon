<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Barber Saloon</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
    <style>
        /* Arka plan ayarları */
        .hero-section {
            position: relative;
            height: 100vh; /* Tam ekran boyutunda */
            background-color: #000; /* siyah arka plan */
        }

        /* Üstteki yazılar için stil */
        .hero-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.6); /* Arka plan karartma efekti */
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            text-align: center;
            color: #fff; /* Beyaz yazı */
        }

            /* Ana başlık */
            .hero-overlay h1 {
                font-size: 4rem;
                font-weight: bold;
                text-transform: uppercase;
                margin-bottom: 20px;
            }

            /* Alt başlık */
            .hero-overlay p {
                font-size: 1.5rem;
                margin-bottom: 30px;
            }

            /* Buton */
            .hero-overlay .btn {
                font-size: 1.2rem;
                padding: 10px 20px;
                border: 2px solid #fff;
                color: #fff;
                background-color: transparent;
                text-transform: uppercase;
                transition: all 0.3s ease-in-out;
            }

                .hero-overlay .btn:hover {
                    background-color: #FF5733; /* Hover sırasında butonun rengi */
                    border-color: #FF5733;
                }

        body {
            background-color: #2c2c2c; /* Light dark arka plan */
            color: #fff; /* Tüm sayfadaki yazı rengi */
        }

        .container {
            max-width: 100%; /* Tam genişlik */
            padding: 0; /* Fazladan boşlukları kaldırır */
            background-color: inherit; /* Body rengini miras alır */
        }


    </style>
</head>
<body>

    <!-- Hero Section -->
    <div class="hero-section">
        <div class="hero-overlay">
            <h1>Trim & Style You</h1>
            <p>Sizin için en iyi bakım ve stil hizmetlerini sunuyoruz!</p>
            <a href="#" class="btn">Randevu Al</a>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
